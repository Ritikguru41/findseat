# ============================================================
#  FindSeat Spring Boot Configuration
# ============================================================

# Server
server.port=8080

# MySQL (local, port 3307)
spring.datasource.url=jdbc:mysql://localhost:3307/findseat?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=66818359
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Email (Gmail SMTP - same settings as the old Node backend)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ritikguru41@gmail.com
spring.mail.password=edlm tngr lgyw tqbt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000

# App URLs
app.frontend.url=http://localhost:5173

# JWT
jwt.secret=findseat_super_secret_jwt_key_2024
jwt.expiration-ms=604800000

# Razorpay
razorpay.key.id=rzp_test_Sm06AUkqqNZFl1
razorpay.key.secret=xUvvPS2PH9Dn2gLuSxcrCEtX

# Seat lock duration (minutes)
app.seat-lock-minutes=5

logging.level.org.hibernate=WARN
